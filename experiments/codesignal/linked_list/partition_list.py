from utils import convertArrayToList, Node, printList, countNodes

def partition(head, value):
    cur = head
    newHead = Node(0)
    n = newHead

def main():
    head = convertArrayToList([1, 4, 3, 2, 5, 2])
    head = partition(head, 3)
    # printList(head)

if __name__ == '__main__':
    main()